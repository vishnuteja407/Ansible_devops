---
- name: user module demo
  hosts: all
  become: true
  vars:
    myuser: "example"
    mygroups: 
      - adm
      - sys
  tasks:
    - name: adding secondary groups
      ansible.builtin.user:
        name: "{{ myuser }}"
        groups: "{{ mygroups }}"
        append: true
